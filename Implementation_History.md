# 🛠️ Implementation History

Minecraft Mod Auto Translator (AI Powered)

**v3.16** (Latest)**Drill-down Retry & Perfect Batching**

- **New** **ドリルダウン・リトライ機能 (Drill-down Retry):** バッチ翻訳中に一部の項目が欠落（翻訳漏れ）した場合、スキップせずに「欠落した項目のみ」を集めたミニバッチを生成し、即座に再翻訳を試みる機能を追加。 これにより、大容量バッチ処理時でも翻訳漏れを許さず、可能な限り 100%の翻訳完了を目指す挙動に進化した。
- **Update** **執念のリトライロジック:** 再試行中に少しでも進捗（1 つでも翻訳できた）があれば、リトライ回数カウントをリセットする仕様に変更。 これにより、難解なファイルであっても、時間をかけて少しずつ確実に翻訳を完了させる粘り強さを獲得。

**v3.15**JSON Auto-Stitch & Batch Rescue

- **Fix** **JSON 連結データの自動修復 (Auto-Stitch):** Gemini が長文翻訳時に複数の JSON オブジェクトを連続して出力してしまう現象（`Extra data` エラー）に対し、それらを自動的に検知・結合して一つの辞書として読み込むロジックを実装。これにより、大容量バッチ時のエラー率が劇的に低下。
- **New** **バッチ部分救済 (Partial Rescue):** トークン制限などで JSON が途中で途切れた場合でも、バッチ全体を破棄せず、読み取れた部分までの翻訳結果を確保して保存するように変更。無駄なリトライを減らし、進捗を確実に進める仕様へ強化。
- **Update** **Markdown 除去の強化:** AI が出力しがちな Markdown コードブロック（````json` 等）を正規表現でより強力かつ安全に除去する処理を追加。

**v3.14**Gemma Integration & Lite Logic Update

- **Update** **Lite 限定モードの定義拡張:** 「Lite モード」において、従来の Gemini Lite モデルに加え、**Gemma 3 系モデル**も使用可能になるようフィルタリングロジックを変更。 Gemma 3 は無料枠での制限が緩く（RPD 14,400）、Lite モデルのスタミナが切れた後の強力なバックアップとして機能する。
- **Fix** **モデル選択ロジックの最適化:** `--lite-only` フラグが指定された際、名前に "lite" または "gemma" が含まれるモデルを通過させるように条件式を修正。

**v3.13**TPM Maximization & JSON Repair

- **New** **TPM 最大活用戦略 (Massive Batching):** Gemini Lite モデルのバッチサイズを劇的に引き上げ（2.5 Lite: 40→300 / 2.0 Lite: 100→400）、RPD（1 日のリクエスト回数）を節約しつつ、余っている TPM（トークン処理能力）を限界まで活用する仕様に変更。これにより処理速度が向上し、429 エラーの発生頻度が激減。
- **Fix** **JSON 構造自動修復 (List-to-Dict Repair):** AI が大量のデータを処理する際、キーを省略して「値のリスト」だけで返してくる現象（dictionary update sequence error）を検知し、自動的に元のキーと結合して正しい辞書形式に復元する救済ロジックを実装。
- **Update** **プロンプト強化 & タイムアウト延長:** AI に対して JSON オブジェクト形式の厳守を強く指示するようにプロンプトを改良。また、大容量バッチの生成待ちに合わせてタイムアウト時間を 60 秒から 90 秒へ延長。

**v3.12**Smart Recovery & Auto-Ban System

- **New** **優先度復帰 (Priority Recovery):** フォールバック（低優先度モデルへの切り替え）中でも、30 分経過すると自動的に優先度の高いモデル（Flash-Lite など）に戻って再試行する機能を追加。これにより、メインモデルのレート制限が解除された際に即座に復帰できるようになった。
- **New** **自動無効化 (Smart Ban):** 10 回連続でエラーが発生したモデルに「使用不可フラグ」を立て、一時的にリストから除外するロジックを実装。応答しないモデルに時間を浪費するのを防ぐ。
- **Update** **全滅救済 (Robust Failover):** 全てのモデルが使用不可になった場合、自動的にフラグをリセットして最初からリトライする安全装置を追加。翻訳プロセスが完全に停止するのを防止。

**v3.11**.env Support & Usability Boost

- **New** **API キーの永続化 (.env 対応):** 起動時に `.env` ファイルを読み込み、API キーを自動設定する機能を追加。初回入力時に自動保存も提案され、毎回のコピペ作業から解放された。
- **New** **ヘルプ機能の実装:** コマンドライン引数 `-h` または `--help` で、ツールの詳細な使い方（PowerShell の標準ヘルプ）を表示する機能を実装。
- **Update** **コード構造の最適化:** `#region` ディレクティブを使用してコードブロックを整理し、メンテナンス性と可読性を向上。

**v3.10**Rate Limit Optimization & Strategy Update

- **Update** **レート制限への適応:** 2025 年 11 月発表の最新レート制限表に基づき、各モデルの制御ロジックを刷新。
- **New** **戦略的バッチサイズ制御:** RPD（1 日のリクエスト数）と TPM（1 分のトークン数）の制約に合わせて動作モードを個別最適化。
  - **Gemini 2.5 Flash-Lite:** RPD 1,000 を活かし、主力として安定稼働（Batch 40）。
  - **Gemini 2.0 Flash-Lite:** 少ない RPD(200)を補うため、超特大バッチ（Batch 100）でリクエスト回数を極限まで節約。
  - **Gemma 3:** 厳しい TPM(15k)制限を回避するため、極小バッチ（Batch 5）で確実に通す「刻み撃ち」設定へ変更。
- **Update** **優先順位の再構成:** スタミナ（RPD）と速度（RPM）のバランスを考慮し、デフォルト順序を `2.5 Lite` > `2.0 Lite` > `2.5 Flash` > `Gemma 3` に最適化。

**v3.9**Pip Install Fix & Robust Error Handling

- **Fix** **ライブラリ定義の修正:** `start.ps1` におけるライブラリ指定を文字列から配列（`@(...)`）に変更。PowerShell が複数のライブラリを単一の引数として誤解釈し、インストールに失敗していた問題を解決。
- **Update** **エラーハンドリング強化:** `pip` コマンドの存在確認と、インストール失敗時の詳細なエラーチェックを追加。エラー発生時に処理を続行せず、適切に停止してユーザーに通知するように改善。
- **Fix** **パス処理の堅牢化:** 空白を含むフォルダパスが渡された際の引用符（`"`）処理を見直し、PowerShell 上でのパス解釈ミスを防ぐよう微調整。

**v3.8**PowerShell Migration & Adaptive Optimization

- **New** **PowerShell 完全移行:** 従来のバッチファイルを廃止し、`start.ps1` と起動用ランチャー `Run_Translator.bat` に刷新。文字化けを解消し、日本語 UI を完全サポート。
- **New** **モデル別最適化:** Gemini 2.5 Lite（RPD 重視・小バッチ高速回転）と 2.0 Lite（TPM 重視・大バッチ節約）の特性に合わせ、バッチサイズと待機時間を動的に制御するロジックを実装。
- **Update** **詳細進捗表示:** 翻訳中のプログレスバーに、進捗率（％）と完了予想時間（ETA）を追加。待機時間も含めた正確な見積もりを表示。
- **Fix** **シェーダー出力修正:** シェーダー翻訳時、出力ファイル名が `.lang` のまま日本語化されるよう修正（例: `ja_JP.lang`）。Windows PowerShell 5.1 以前の OS 判定バグも修正。

**v3.7**Gemini 2.5 Preview & Gemma 3 Integration

- **New** **最新モデル対応:** `gemini-2.5-flash-lite-preview-09-2025` および `gemini-2.0-flash-lite-preview-02-05` を優先リストに追加。
- **New** **Gemma 3 サポート:** Google API 経由で利用可能な `gemma-3` シリーズ（1b ～ 27b）および `gemma-3n` 系モデルを自動選択ロジックに統合。
- **Update** **優先順位の最適化:** 最新の Lite モデルと Gemma 3 を最優先に使用するようデフォルト順序を更新し、効率をさらに向上。

**v3.6**Shader Support & Path Fixes

- **New** **シェーダーパック翻訳対応:** `.lang` ファイル（key=value 形式）の読み書きに対応し、シェーダーパックの翻訳が可能になった。
- **Update** **モード自動切替:** 入力フォルダ名に「shader」が含まれる場合、自動的にシェーダー翻訳モードに切り替わり、出力構造やフォーマットが最適化される。
- **Fix** **出力パス修正:** シェーダーモード時は、Mod のような ID ごとの分類ではなく、zip 内のディレクトリ構造を維持して出力するように修正。

**v3.5**Enhanced Cleaning & Re-translation

- **New** **出力ファイル浄化機能:** ツール起動時に、すでに出力された `ja_jp.json` を全スキャンし、「Error 504」などのエラー文字列が含まれるエントリを物理的に削除する機能を追加。
- **Update** **再翻訳トリガー:** 出力ファイルから削除された（またはエラーが含まれる）エントリは、次回の実行時に自動的に再翻訳対象となるようロジックを強化。
- **Update** **最終防衛ライン:** 翻訳結果をファイルに書き込む直前にも再チェックを行い、エラー文字列が含まれる場合は原文（英語）に戻して保存するように変更。

**v3.4**Lite Strategy & Self-Healing

- **New** **Lite 限定モード:** `--lite-only` オプションを追加。レート制限対策として、軽量モデルのみに限定して動作させる機能。
- **New** **自動修復機能 (Self-Healing):** 起動時に出力済みファイル（`ja_jp.json`）とキャッシュを全スキャンし、「Error 504」などの汚染データを物理的に削除して再翻訳対象にする機能。
- **Update** **優先順位の刷新:** RPD（1 日あたりのリクエスト数）を重視。Gemini 2.5 Lite (RPD 1000) を主力とし、Gemma 3 (RPD 14400) を「最後の砦」として配置する鉄壁の布陣へ変更。
- **New** **詳細進捗可視化:** ファイル単位だけでなく、内部のバッチ通信状況（`Translating: 1/86`）をプログレスバーで可視化し、フリーズの誤解を解消。
- **New** **モデルチェッカー:** 引数なしで起動した際に、API キーで利用可能なモデル一覧を表示する機能を追加。
- **Fix** **429 エラー対策強化:** レート制限時は待機せず即座に次のモデルへスイッチし、全モデル枯渇時のみ待機するロジックに変更。

**v3.3**Custom Path Support

- **New** **出力先自由指定:** `start.bat` で出力先フォルダのフルパス指定やドラッグ＆ドロップ入力に対応。デスクトップや別ドライブへの保存が可能に。
- **Fix** **パス表示改善:** 実行ログに出力先の絶対パスを表示し、保存場所を明確化。

**v3.2**Manual Selection & Project Isolation

- **Update** **プロジェクト別管理:** キャッシュとログの保存場所を、スクリプト直下から「各出力リソースパックフォルダ内」に変更。複数プロジェクトの並行作業が可能に。
- **New** **手動モデル選択モード:** 自動選択に頼らず、API キーで利用可能なモデル一覧からユーザーが直接番号で選べる機能を追加。

**v3.1**Statistics & Visibility

- **New** **詳細統計表示:** フォルダ内の全ファイル数、Mod ファイル数、スキップ済み数を明確に表示し、「Mod 数が合わない」問題を解決。
- **Update** **探索ロジックの適正化:** 再帰探索（サブフォルダ探索）を廃止し、ユーザーの期待値に近い「直下フォルダのみ」の探索に戻した。
- **Fix** **Mod 認識修正:** `.jar` に加えて `.zip` 形式のファイルも正しく認識するように修正。

**v3.0**REST API & Stability Overhaul

- **Fix** **通信方式の刷新 (REST API 化):** フリーズやクラッシュの原因となっていた公式 SDK (gRPC) を廃止し、Python 標準の `requests` を用いた REST API 直接通信へ完全移行。これにより安定性が劇的に向上。
- **New** **モデル自動探索機能:** 決め打ちのモデル名ではなく、API キーを使って「現在利用可能なモデル一覧」を取得し、最適なものを自動で選ぶロジックを実装（404 エラー根絶）。
- **New** **デバッグ詳細表示:** API エラー時に HTTP ステータスコードとメッセージ全文を表示するように変更。

**v2.5**Fallback System & Batch Fixes

- **New** **Google 翻訳フォールバック:** AI 翻訳がエラー（404/429 等）になった際、処理を止めずに Google 翻訳へ自動で切り替える機能を実装。
- **New** **リアルタイム可視化:** プログレスバーにフォールバック発生回数をリアルタイムで表示。
- **Fix** **バッチファイル安定化:** 日本語コメントによる文字化け、`&` 記号による構文エラー、クリップボード誤爆問題を修正（`choice`コマンド導入、英語 UI 化）。

**v2.0**AI Integration

- **New** **Gemini AI 搭載:** 単語置換ではなく、JSON をまとめて AI に投げ、文脈（Minecraft 用語）を考慮した翻訳を実現。
- **New** **バージョン自動検知:** Mod 内の `pack.mcmeta` を解析し、最適なパックフォーマットを自動設定。

**v1.0**Initial Release

- **New** **基本機能:** Mod からの言語ファイル抽出、Google 翻訳、リソースパック生成、キャッシュ機能。
- **New** **環境構築:** `start.bat` による Python 仮想環境(venv)の自動構築。

Created by 賢狼 (Wise Wolf) with Gemini
